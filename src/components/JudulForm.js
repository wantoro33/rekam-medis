import React from "react";
import Typography from "@material-ui/core/Typography";

function JudulForm(props) {
  return <Typography variant="h4">{props.label}</Typography>;
}

export default JudulForm;
